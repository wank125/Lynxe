# Multi-Step Func-Agent Examples - 使用指南

本文档提供了三个完整的多步骤 Func-Agent 示例,展示了如何构建复杂的自动化工作流。

## 📦 文件说明

**文件**: `multi-step-func-agent-examples.json`

包含三个完整的工作流示例:
1. **数据分析工作流** (5步) - 数据收集 → 验证 → 分析 → 可视化 → 报告
2. **网络研究工作流** (5步) - 规划 → 搜索 → 提取 → 综合 → 报告
3. **代码审查工作流** (5步) - 发现 → 质量 → 安全 → 性能 → 报告

---

## 🚀 快速开始

### 方式 1: 通过 UI 导入

1. 启动 Lynxe: `http://localhost:18080`
2. 点击右侧面板的 **"导入现有计划"** 按钮
3. 选择 `multi-step-func-agent-examples.json` 文件
4. 系统会自动导入所有三个工作流

### 方式 2: 通过 API 导入

```bash
curl -X POST http://localhost:18080/api/plan-template/import \
  -H "Content-Type: application/json" \
  -d @example/multi-step-func-agent-examples.json
```

---

## 📊 示例 1: 数据分析工作流

### 工作流概述

```
Step 1: 数据收集
   ↓ (传递: data_summary)
Step 2: 数据验证
   ↓ (传递: validation_report)
Step 3: 数据分析
   ↓ (传递: analysis_report)
Step 4: 可视化规划
   ↓ (传递: visualization_plan)
Step 5: 最终报告生成
   ↓ (输出: final_report)
```

### 使用方法

#### 通过 HTTP API 调用

```bash
# 1. 上传文件 (需要获取 uploadKey)
curl -X POST http://localhost:18080/api/file-upload/upload \
  -H "Content-Type: multipart/form-data" \
  -F "files=@/path/to/your/data.csv"

# 2. 执行工具 (使用 uploadKey)
curl -X POST http://localhost:18080/api/executor/executeByToolNameAsync \
  -H "Content-Type: application/json" \
  -d '{
    "toolName": "data-analysis-workflow",
    "serviceGroup": "data-analysis",
    "uploadKey": "upload-xxxxxxxxx", 
    "replacementParams": {
      "file_path": "data.csv",
      "output_path": "analysis_report.md"
    }
  }'
```

#### 通过内部工具调用

```json
{
  "toolName": "data-analysis-data-analysis-workflow",
  "parameters": {
    "file_path": "/Users/username/data/sales_data.csv",
    "output_path": "/Users/username/reports/analysis_report.md"
  }
}
```

### 参数说明

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `file_path` | string | ✅ | 要分析的数据文件路径 |
| `output_path` | string | ✅ | 最终报告保存路径 |

### 步骤详解

#### Step 1: 数据收集
- **工具**: `fs-read-file-operator`
- **任务**: 读取数据文件,识别格式,统计记录数
- **输出**: `data_summary` - 包含数据概览和样本

#### Step 2: 数据验证
- **工具**: `default-terminate`
- **任务**: 验证数据质量,检查缺失值,计算完整性
- **输出**: `validation_report` - 数据质量评分和问题列表

#### Step 3: 数据分析
- **工具**: `default-terminate`
- **任务**: 统计分析,计算关键指标,识别趋势和异常
- **输出**: `analysis_report` - 统计发现和洞察

#### Step 4: 可视化规划
- **工具**: `default-terminate`
- **任务**: 设计可视化方案,推荐图表类型
- **输出**: `visualization_plan` - 可视化建议

#### Step 5: 最终报告
- **工具**: `fs-write-file-operator`
- **任务**: 整合所有结果,生成综合报告
- **输出**: `final_report` - 完整的分析报告文件

---

## 🔍 示例 2: 网络研究工作流

### 工作流概述

```
Step 1: 搜索规划
   ↓ (传递: search_plan)
Step 2: 网络搜索
   ↓ (传递: search_results)
Step 3: 内容提取
   ↓ (传递: extracted_content)
Step 4: 信息综合
   ↓ (传递: synthesis_report)
Step 5: 研究报告生成
   ↓ (输出: final_research_report)
```

### 使用方法

```bash
curl -X POST http://localhost:18080/api/executor/executeByToolNameAsync \
  -H "Content-Type: application/json" \
  -d '{
    "toolName": "web-research-workflow",
    "serviceGroup": "web-research",
    "replacementParams": {
      "topic": "AI Agent 最新发展趋势",
      "output_file": "research_report.md"
    }
  }'
```

### 参数说明

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `research_topic` | string | ✅ | 研究主题 |
| `output_file` | string | ✅ | 研究报告保存路径 |

### 步骤详解

#### Step 1: 搜索规划
- **任务**: 分析研究主题,制定搜索策略
- **输出**: 3-5个具体搜索查询和研究方法

#### Step 2: 网络搜索
- **工具**: `web-search`
- **任务**: 执行搜索,收集相关URL和摘要
- **输出**: 前10个相关来源

#### Step 3: 内容提取
- **工具**: `browser-navigate`, `browser-extract-content`
- **任务**: 访问网页,提取关键信息
- **输出**: 每个来源的摘要和关键事实

#### Step 4: 信息综合
- **任务**: 综合所有信息,解决冲突,组织发现
- **输出**: 按主题组织的综合报告

#### Step 5: 研究报告
- **工具**: `fs-write-file-operator`
- **任务**: 生成完整研究报告,包含引用
- **输出**: 格式化的研究报告文件

---

## 💻 示例 3: 代码审查工作流

### 工作流概述

```
Step 1: 代码发现
   ↓ (传递: discovery_report)
Step 2: 代码质量分析
   ↓ (传递: quality_analysis)
Step 3: 安全审查
   ↓ (传递: security_report)
Step 4: 性能分析
   ↓ (传递: performance_report)
Step 5: 综合审查报告
   ↓ (输出: final_review_report)
```

### 使用方法

```bash
curl -X POST http://localhost:18080/api/executor/executeByToolNameAsync \
  -H "Content-Type: application/json" \
  -d '{
    "toolName": "code-review-workflow",
    "serviceGroup": "code-review",
    "uploadKey": "upload-xxxxxxxxx",
    "replacementParams": {
      "source_file_path": "Main.java",
      "report_output_path": "review_report.md"
    }
  }'
```

### 参数说明

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `project_path` | string | ✅ | 项目目录路径 |
| `report_output_path` | string | ✅ | 审查报告保存路径 |

### 步骤详解

#### Step 1: 代码发现
- **工具**: `fs-list-directory`, `fs-read-file-operator`
- **任务**: 扫描项目,识别代码文件,构建结构图
- **输出**: 项目结构概览

#### Step 2: 代码质量分析
- **任务**: 分析代码质量,检查代码异味,评估复杂度
- **输出**: 质量评分和改进建议

#### Step 3: 安全审查
- **任务**: 识别安全漏洞,检查常见安全问题
- **输出**: 安全问题列表和修复建议

#### Step 4: 性能分析
- **任务**: 识别性能瓶颈,分析算法效率
- **输出**: 性能问题和优化建议

#### Step 5: 综合报告
- **工具**: `fs-write-file-operator`
- **任务**: 整合所有审查结果,按优先级排序
- **输出**: 完整的代码审查报告

---

## 🔧 自定义和扩展

### 修改步骤数量

您可以根据需要添加或删除步骤:

```json
{
  "steps": [
    {
      "stepRequirement": "新步骤的要求...",
      "selectedToolKeys": ["tool1", "tool2"],
      "terminateColumns": "output_variable_name"
    }
  ]
}
```

### 参数传递机制

**核心机制：隐式上下文传递 (Implicit Context)**

Lynxe 的 Func-Agent（基于 ReAct 模式）会自动将上一步的执行结果（Observation）放入对话上下文中。因此，在后续步骤中，您不需要使用显式的变量语法（如 `<<var>>`），只需用自然语言引用即可。

**✅ 正确做法：**

```
Step 1: ... call default-terminate with a summary ...
Step 2: "Based on the data summary from the previous step..."
```

**❌ 错误做法 (会导致参数验证失败)：**

```
Step 2: "Based on: <<data_summary>>" 
// 注意：系统会将 <<...>> 识别为必须在 API 调用时传入的初始参数！
```

### 工具选择

每个步骤可以选择多个工具:

```json
"selectedToolKeys": [
  "fs-read-file-operator",
  "fs-write-file-operator",
  "web-search",
  "default-terminate"
]
```

### 调整最大步数

控制每个步骤的最大执行轮数:

```json
"maxSteps": 10  // 每个步骤最多执行10轮 Think-Act 循环
```

---

## 📝 最佳实践

### 1. 清晰的步骤要求

每个步骤的 `stepRequirement` 应该:
- ✅ 明确说明任务目标
- ✅ 列出具体的执行步骤
- ✅ 指定期望的输出格式
- ✅ 引用前序步骤的输出

### 2. 合理的工具选择

- 只选择当前步骤真正需要的工具
- 始终包含 `default-terminate` 用于步骤完成
- 文件操作使用 `fs-*` 系列工具
- 网络操作使用 `web-search` 或 `browser-*` 工具

### 3. 参数命名规范

- 使用描述性的参数名: `file_path` 而不是 `path`
- 输出变量名应反映内容: `analysis_report` 而不是 `output`
- 保持命名一致性

### 4. 错误处理

在步骤要求中包含错误处理指导:

```
如果文件不存在,返回错误信息并建议检查路径
如果数据格式不支持,列出支持的格式
```

---

## 🎯 执行和监控

### 查看执行进度

```bash
# 获取执行详情
curl http://localhost:18080/api/executor/details/{planId}

# 获取步骤详情
curl http://localhost:18080/api/executor/agent-execution/{stepId}
```

### 实时监控

在 Lynxe UI 中:
1. 左侧面板显示执行历史
2. 点击任务查看详细进度
3. 右侧面板显示每个步骤的 Think-Act 记录

### 调试技巧

1. **查看 Think 输入**: 了解 LLM 接收到的完整上下文
2. **查看 Think 输出**: 理解 LLM 的推理过程
3. **查看工具调用**: 确认工具参数和返回结果
4. **检查参数传递**: 验证步骤间数据流转正确

---

## 🌟 进阶用法

### 嵌套工作流

一个工作流可以调用另一个工作流:

```json
"selectedToolKeys": [
  "data-analysis-data-analysis-workflow",  // 调用数据分析工作流
  "default-terminate"
]
```

### 条件执行

在步骤要求中添加条件逻辑:

```
如果数据质量分数 > 80,继续分析
如果数据质量分数 < 80,先执行数据清洗工具
```

### 并行工具调用

启用并行工具调用以提高效率:

```json
"selectedToolKeys": [
  "web-search",
  "database-query",
  "file-read"
]
// LLM 可以同时调用多个工具
```

---

## 📚 相关资源

- **官方文档**: [README-dev.md](../README-dev.md)
- **更多示例**: [Lynxe Public Prompts](https://github.com/Lynxe-public/Lynxe-public-prompts)
- **API 文档**: 访问 `http://localhost:18080/swagger-ui.html`

---

## 💡 常见问题

### Q: 如何修改工作流?

A: 导入后,在 Lynxe UI 中选择工作流,点击编辑按钮,修改 JSON 配置。

### Q: 参数验证失败 (Missing parameters) 怎么办?

A: 这通常是因为您在 `stepRequirement` 中使用了 `<<variable>>` 语法引用中间变量。这是**不被支持**的。
- 系统会将 JSON 中所有的 `<<variable>>` 视为**外部输入参数**（必须在 API 调用时提供）。
- 对于步骤间的数据传递，请直接使用**自然语言**描述（例如 "Analyze the output from step 1"）。

### Q: 找不到文件 (File not found) 或无法读取文件?

A: Lynxe 后端运行在沙箱环境中，通常无法直接访问宿主机的绝对路径。
- **解决方法**：使用 `/api/file-upload/upload` 接口上传文件，获取 `uploadKey`。
- 在执行请求中包含 `"uploadKey": "..."`。
- 将文件路径参数设置为**相对文件名**（例如 `data.csv`），而不是绝对路径。

### Q: 如何添加自定义工具?

A: 在 `selectedToolKeys` 中添加工具名称,确保该工具已在系统中注册。

### Q: 工作流执行太慢?

A: 考虑:
1. 减少 `maxSteps` 限制
2. 启用并行工具调用
3. 优化步骤要求,减少不必要的处理

---

## 🎉 开始使用

现在您可以:
1. 导入示例文件
2. 根据需求选择合适的工作流
3. 修改参数并执行
4. 查看执行过程和结果
5. 根据需要自定义和扩展

祝您使用愉快! 🚀
